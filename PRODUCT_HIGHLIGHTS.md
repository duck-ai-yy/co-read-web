# Co-Read · 产品亮点笔记

> 鸭鸭在开发过程中陆续发现的"亮点"——做产品宣传 / 落地页 / pitch deck 时用。
> 每条含：① 用户感知层（讲给用户听） ② 技术实现层（讲给同行 / 技术博客） ③ 营销话术候选

---

## 🎨 思考跟着颜色走（Color-tagged Cognition）

发现于 2026-05-19 v2-a 开发期间

### ① 用户感知层
PDF 上每种颜色不只是"做标记"，而是**对 AI 的指令**——
- 🔴 红 · 不懂 → AI 自动详细解释，假设你没基础
- 🟢 绿 · 赞同 → AI 简短确认或补充延伸
- 🔵 蓝 · 与课题相关 → AI 联想你的研究方向，讨论应用
- 🟣 紫 · 质疑 → AI 扮演批判视角，找此论点的弱点
- 🟡 黄 · 重点 → AI 仅记录，不主动展开
- ⚪ 灰 · 待查 → AI 仅留存，后续追问

跟 ChatGPT / Perplexity 完全不同的 mental model：**高亮是 first-class，对话依附于高亮**——研究者的工作单位是"段落 + 围绕它的思考"，不是"线性会话"。

### ② 技术实现层
- 颜色作为"低带宽语义信号"传给 LLM context（不用打字 = 用 UI affordance 替代 prompt）
- `system_prompt.md` 定义每种颜色对应的回应风格
- 每个高亮对应一个独立 thread，**删高亮 = 删对应思考**——保持心智干净

### ③ 营销话术候选
- "划线即追问。颜色即指令。"
- "你的思考方式，AI 自动理解。"
- "六种颜色，六种 AI 人格——你选谁陪你读。"
- "不是再做一个 ChatPDF——这是把研究者的工作流，变成颜色的语言。"

---

## 💰 永远命中缓存的对话经济学（Cache-Native Conversation）

发现于 2026-05-18 切换 DeepSeek 时

### ① 用户感知层
- **一篇论文聊一整天 < 1 元人民币**
- 不订阅、按需付费、不浪费
- 比 ChatGPT Plus 月费便宜 95%+

### ② 技术实现层
- D 策略：每次请求复用同一份 system prompt + PDF 全文（稳定前缀）
- DeepSeek 服务端自动识别"前缀完全相同" → 缓存命中
- 输入 token 价格自动从 $0.28/M → **$0.028/M（90% 折扣）**
- 命中率随对话轮数指数级提升——开始一篇论文聊 10 轮，第 10 轮几乎免费
- 用户完全无感知，无需配置

### ③ 营销话术候选
- "深读一篇论文，只花一颗糖的钱。"
- "Cache-native：让每一次思考都几乎免费。"
- "不订阅，按需用。读 100 篇论文比 ChatGPT Plus 一个月还便宜。"
- "这不是省钱——这是让 AI 阅读变成基础设施。"

---

## 🔗 可追溯的论文引用（Trace-Back Citations）

发现于 2026-05-19 v3 设计期间

### ① 用户感知层
- agent 回答里凡是引用论文的内容**必带回链标记**（如 `〔p.5〕`）
- 鼠标 hover 显示下划线（暗示可点击）→ 离开消失（不打扰阅读）
- 点击 → PDF viewer 自动滚到该页 + 视觉闪烁定位
- **再也不用怀疑 AI 是不是编的——每个观点能秒回原文**

### ② 技术实现层
- system prompt 强制 agent 在引用论文内容时输出 `〔p.N〕` 标记
- marked.js 自定义 renderer：解析 `〔p.N〕` → 渲染为 `<a class="cite-link">` + 注入 click handler
- 点击 → `pdfViewer.scrollPageIntoView({pageNumber:N})` + 可选 `findController` 文本精确定位 + hl-flash 动画

### ③ 营销话术候选
- "AI 引用，可以回跳。"
- "学术严谨从交互开始。"
- "不是 AI 编的——每句都能追溯到原文。"

---

## 🎯 主题（Topic）= 研究流容器

发现于 2026-05-19 v3 设计期间

### ① 用户感知层
- 一个**主题** = 一组论文 + **一套冻结色板** + 一份导出笔记
- 创建主题时定义色板含义（基于默认改）→ 主题创建后**完全不可改**
- 每个研究方向开一个主题，颜色语言对应你的思维框架
- 整理笔记时 6 篇论文用同样颜色 = 同样语义，输出 .md 不混乱

### ② 技术实现层
- 项目数据模型：Topic → many PDF → many annotations + threads
- 色板冻结 = system message 前缀稳定 = **跨论文 caching 命中**（一个主题内打开第 N 篇论文，system 部分已经在 LLM 缓存里了，只算 10% 价格）
- 数据持久化在 IndexedDB，跨 session 一致

### ③ 营销话术候选
- "给你的研究装一个容器。"
- "每个主题都有自己的颜色语言。"
- "一篇论文聊一下午——一个主题省一周。"

---

## 📝 待发现 / 鸭鸭后续会加

（开发过程中陆续发现新亮点，告诉主 Claude，会追加到本文件）
