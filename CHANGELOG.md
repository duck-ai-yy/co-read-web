# 更新日志

版本号遵循语义化版本 `major.minor.patch`。`main` 分支 = 线上版（Render 自动部署）。

## v0.1.0 — 2026-05-22 · 首个试用版（5 人定向试用基线）

co-read 论文伴读的首个对外试用版本。

- **阅读 + comment**：划线选色 = 给段落打标签；标记后就地写批注，输入 `@AI` 召唤 AI 围绕原文讨论；右栏 comment 列表（手风琴两级折叠）。
- **默认色卡**：没懂 / 重点 / 可借鉴 / 存疑 / 要引用 / 待查 —— 统一按"读完这段要拿它干什么"组织（旧色卡混了理解状态/相关性/重要性多个维度，划线时易选不准）。首次进入自动弹 3 步弹框，引导用户确认/微调色卡含义。
- **导出**：把"收入笔记"的内容按标签归类导出为 Markdown（独立导出页，可逐条勾选）。
- **引导页** `/guide.html`：图文介绍玩法。**试用埋点** `/api/event`：只记行为信号、不记任何内容/PII。
- **部署**：Render（Singapore），DeepSeek 驱动。

留待 v0.2：Obsidian 知识库导出（PDF + 彩色 callout + 自动 wikilink）、citation 自动生成、导出打包 zip。
